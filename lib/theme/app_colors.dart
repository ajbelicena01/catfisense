import 'package:flutter/material.dart';

const kBrandOrange = Color(0xFFF6A242);

// Dark mode palette. Roles: kDarkBackground is the page canvas, kDarkSurface
// is for cards/header/nav chrome, kDarkBorder doubles as dividers and muted
// text, kDarkPrimary replaces kBrandOrange as the dark-mode brand accent, and
// kDarkSecondary/kDarkAccent are the two pop accents for icons and highlights.
const kDarkBackground = Color(0xFF2B2D3B);
const kDarkSurface = Color(0xFF494C65);
const kDarkBorder = Color(0xFF676386);
const kDarkPrimary = Color(0xFFDA935D);
const kDarkSecondary = Color(0xFFA5B2EB);
const kDarkAccent = Color(0xFFDDDE68);
